#!/bin/bash
# The line above is called a "shebang".
# It tells the system to use the Bash shell to run this script.

# This is a comment. It is ignored by the shell.
# Comments are used to explain what the script is doing.

# The 'echo' command is used to print text to the terminal.
# Let's use it to display a message:

echo "Hello Shell-Scripting"
